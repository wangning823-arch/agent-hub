# Phase 0 Frontend Design (概要)

- 明确前端分层：Header、Sidebar、ChatPanel、RightPanel、Modal、Toast 等组件的职责边界。
- 初步规划自定义 Hook：useSessions、useAgents、useViewport、useAuth 的接口和初步实现路径。
- 逐步引入 TypeScript 的迁移计划与优先级（先选核心数据结构的类型定义）。
- 保留现有渲染行为，作为阶段1落地的安全前置。 
